"""Student starter: implement reusable classification metrics here."""


def evaluate(y_true: list[str], y_pred: list[str]) -> dict:
    """Calculate evaluation metrics and return them in a dictionary.

    TODO: begin with accuracy, then add invalid counts, per-label metrics,
    macro-F1, and confusion counts in later modules.
    """
    raise NotImplementedError("Implement the evaluation function")
